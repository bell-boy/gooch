{
  "tasks": [
    {
      "type": "shell",
      "label": "make build/test/${fileBasenameNoExtension}.run",
      "command": "make",
      "args": ["build/test/${fileBasenameNoExtension}.run"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$gcc"]
    }
  ],
  "version": "2.0.0"
}